extends layout

block content
  h1 Mua sản phẩm

  form(method='POST' action='')
    button.btn.btn-primary(type='submit') Mua hàng

  if errors
    ul
      for error in errors
        li!= error.msg
