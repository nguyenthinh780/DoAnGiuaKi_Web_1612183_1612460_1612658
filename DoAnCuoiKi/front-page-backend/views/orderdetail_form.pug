extends layout

block content
  h1 Thêm sản phẩm vào giỏ hàng:
  //h4 Tên sản phẩm: #{product_add.product_name}

  form(method='POST' action='')
    div.form-group
      label(for='number') Số lượng:
      input#number.form-control(type='text' placeholder='Số lượng bạn muốn mua' name='number' required='true' value=(undefined===orderdetail ? '' : orderdetail.number) )
    button.btn.btn-primary(type='submit') Submit

  if errors
    ul
      for error in errors
        li!= error.msg
